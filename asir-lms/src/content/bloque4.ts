import type { TopicContent } from "./types";

// Bloque 4 · contenido en preparación (estructura en types.ts, ejemplo completo en bloque1.ts).
export const bloque4: Record<number, TopicContent> = {};
