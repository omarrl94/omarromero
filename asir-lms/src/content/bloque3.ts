import type { TopicContent } from "./types";

// Bloque 3 · contenido en preparación (estructura en types.ts, ejemplo completo en bloque1.ts).
export const bloque3: Record<number, TopicContent> = {};
