import type { TopicContent } from "./types";

// Bloque 5 · contenido en preparación (estructura en types.ts, ejemplo completo en bloque1.ts).
export const bloque5: Record<number, TopicContent> = {};
