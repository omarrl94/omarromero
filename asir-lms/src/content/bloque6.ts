import type { TopicContent } from "./types";

// Bloque 6 · contenido en preparación (estructura en types.ts, ejemplo completo en bloque1.ts).
export const bloque6: Record<number, TopicContent> = {};
